console.log(" printFunc");

setTimeout(() => 1{
console.log("Please enter number");
}, 1000);

console.log("Please enter one digit number");

promise
.then((number) => number + 10)
.then((result) => console.log(result))

.then((number) => number + 12)
.then((result) => console.log(result))

.then((number) => number + 35)
.then((result) => console.log(result))

then ((number => number + 20)
.then((result) => console.log(result))

.then((number) => number + 30)
.then((result) => console.log(result))

do {
    sum += checkNumber 
    checkNumber++
  } while (checkNumber 

    function fetchData() {
        return new Promise((resolve) => {
          setTimeout(() => {
            resolve({
              name: "John Doe",
              age: 30,
              email: "john@example.com",
            })
          }, 2000)
        })
        
function displayUserDetails (displayUserDetails)
    console.log(displayUserDetails)
      }
      let count = 0; 
  
const intervalId = setInterval(() => { 
  console.log('luka'); 
  count++; 